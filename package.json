{
  "name": "strangesongs-site",
  "version": "1.0.0",
  "description": "Personal tracking site for films, books, and live performances",
  "main": "build.js",
  "scripts": {
    "build": "node build.js",
    "serve": "python3 -m http.server 8090",
    "watch": "nodemon --watch content --watch templates --watch build.js --watch style.css --watch photography.js --exec 'node build.js'",
    "build-album-shards": "node scripts/build-album-shards.js",
    "validate-photography": "node scripts/validate-photography-manifests.js",
    "deploy": "npm run build && git add -A && git commit -m \"Update content\" && git push"
  },
  "dependencies": {
    "ejs": "^3.1.9"
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  }
}
